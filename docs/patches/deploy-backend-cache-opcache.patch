diff --git a/.github/workflows/deploy-backend.yml b/.github/workflows/deploy-backend.yml
index aeaaf9a..40b7377 100644
--- a/.github/workflows/deploy-backend.yml
+++ b/.github/workflows/deploy-backend.yml
@@ -24,8 +24,14 @@ jobs:
             git fetch origin main
             git reset --hard origin/main
             cd backend
-            CACHEBUST=$(git rev-parse HEAD) docker compose up -d --build
+            CACHEBUST=$(git rev-parse HEAD) docker compose up -d --build --force-recreate
             sleep 5
             docker compose exec -T app php artisan config:clear
             docker compose exec -T app php artisan migrate --force
-            docker compose exec -T app php artisan db:seed --force
\ No newline at end of file
+            docker compose exec -T app php artisan db:seed --force
+            # Bake Laravel's cached config/routes/views. These must run after
+            # .env/migrate and after the container is recreated because
+            # OPcache has validate_timestamps=0 in production.
+            docker compose exec -T app php artisan config:cache
+            docker compose exec -T app php artisan route:cache
+            docker compose exec -T app php artisan view:cache
