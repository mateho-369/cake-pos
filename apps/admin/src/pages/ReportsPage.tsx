import { useState } from 'react'
import { ArrowDownRight, ArrowUpRight, CalendarDays, Download, FileSpreadsheet, Lightbulb, TrendingUp } from 'lucide-react'
import { useAdminData } from '../lib/data'
import { translateCategory, useTranslation } from '../lib/i18n'

export default function ReportsPage({ onToast }: { onToast: (message: string) => void }) {
  const { t } = useTranslation(); const { categories, products, revenueData } = useAdminData(); const [tab, setTab] = useState('sales')
  const tabs = [{ id: 'sales', label: 'reports.sales' }, { id: 'products', label: 'reports.products' }, { id: 'payments', label: 'reports.payments' }, { id: 'team', label: 'reports.team' }, { id: 'waste', label: 'reports.waste' }]
  const libraries = [{ key: 'dailySummary', label: 'reports.dailySummary' }, { key: 'sellThrough', label: 'reports.sellThrough' }, { key: 'reconciliation', label: 'reports.reconciliation' }, { key: 'shiftVariance', label: 'reports.shiftVariance' }, { key: 'freshWaste', label: 'reports.freshWaste' }, { key: 'employeePerformance', label: 'reports.employeePerformance' }]
  return <div className="page-content"><section className="reports-header"><div className="filter-tabs report-tabs">{tabs.map((item) => <button key={item.id} className={tab === item.id ? 'active' : ''} onClick={() => setTab(item.id)}>{t(item.label)}</button>)}</div><div className="toolbar-actions"><button className="secondary-button"><CalendarDays size={16} /> {t('reports.dateRange')}</button><button className="primary-button" onClick={() => onToast(t('reports.prepared', { name: t(`reports.${tab}`) }))}><Download size={16} /> {t('reports.export')}</button></div></section><section className="report-kpi-row"><ReportKpi label={t('dashboard.netSales')} value="$6,658" change="+14.2%" up detail={t('reports.vsPrevious')} /><ReportKpi label={t('reports.grossProfit')} value="$4,126" change="+11.8%" up detail={t('reports.marginDetail')} /><ReportKpi label={t('dashboard.orders')} value="268" change="+8.7%" up detail={t('reports.averageDetail')} /><ReportKpi label={t('reports.waste')} value="$92" change="−18.3%" up detail={t('reports.netSalesPercent')} /></section><section className="report-main-grid"><div className="glass-panel report-chart-card"><div className="panel-heading"><div><span className="section-kicker">{t('reports.comparison')}</span><h2>{tab === 'waste' ? t('reports.wasteTrend') : t('reports.salesProfit')}</h2></div><div className="dual-legend"><span><i className="sales" />{t('reports.sales')}</span><span><i className="profit" />{t('reports.grossProfit')}</span></div></div><ComparisonChart waste={tab === 'waste'} /></div><div className="glass-panel insight-panel"><div className="insight-icon"><Lightbulb size={20} /></div><span className="section-kicker">{t('reports.ownerInsight')}</span><h2>{t('reports.opportunity')}</h2><p>{t('reports.opportunityText')}</p><div className="insight-metric"><TrendingUp size={18} /><div><strong>{t('reports.recommended')}</strong><span>{t('reports.recommendedText')}</span></div></div><button className="secondary-button full-button" onClick={() => onToast(t('reports.recommendationSaved'))}>{t('reports.saveRecommendation')}</button></div></section><section className="report-bottom-grid"><div className="glass-panel category-report"><div className="panel-heading"><div><span className="section-kicker">{t('reports.contribution')}</span><h2>{t('reports.salesCategory')}</h2></div><button className="text-button">{t('common.viewBreakdown')}</button></div><div className="category-report-list">{categories.map((category, index) => <div key={translateCategory(t, category.name)}><span className="rank">{index + 1}</span><div><strong>{translateCategory(t, category.name)}</strong><i><b style={{ width: `${category.revenue / 19}%`, background: category.color }} /></i></div><span><strong>${category.revenue.toLocaleString()}</strong><small>{Math.round(category.revenue / 56)}%</small></span></div>)}</div></div><div className="glass-panel margin-report"><div className="panel-heading"><div><span className="section-kicker">{t('reports.menuEngineering')}</span><h2>{t('reports.profitability')}</h2></div></div><div className="margin-row table-head"><span>{t('dashboard.product')}</span><span>{t('dashboard.revenue')}</span><span>{t('reports.margin')}</span></div>{products.slice(0, 5).map((product) => <div className="margin-row" key={product.id}><div className="catalog-product"><span className="catalog-image small" style={{ backgroundPosition: product.imagePosition }} /><strong>{product.name}</strong></div><strong>${product.revenue}</strong><span className={`margin-pill ${product.id === 4 ? 'amber' : ''}`}>{product.id === 4 ? '54%' : `${64 + product.id}%`}</span></div>)}</div></section><section className="glass-panel report-library"><div className="panel-heading"><div><span className="section-kicker">{t('reports.downloads')}</span><h2>{t('reports.library')}</h2></div></div><div className="report-library-grid">{libraries.map((item) => <button key={item.key} onClick={() => onToast(t('reports.prepared', { name: t(item.label) }))}><FileSpreadsheet size={19} /><span><strong>{t(item.label)}</strong><small>{t('reports.updatedNow')}</small></span><Download size={16} /></button>)}</div></section></div>
}
function ReportKpi({ label, value, change, up, detail }: { label: string; value: string; change: string; up: boolean; detail: string }) { return <article className="glass-panel report-kpi"><span>{label}</span><strong>{value}</strong><div className={up ? 'green-text' : 'coral-text'}>{up ? <ArrowUpRight size={14} /> : <ArrowDownRight size={14} />}<b>{change}</b><small>{detail}</small></div></article> }
function ComparisonChart({ waste }: { waste: boolean }) { const { t } = useTranslation(); const { revenueData } = useAdminData(); const days = ['mon', 'tue', 'wed', 'thu', 'fri', 'sat', 'sun']; return <div className="comparison-chart"><div className="bar-y-labels"><span>$1.5k</span><span>$1k</span><span>$500</span><span>$0</span></div><div className="bar-plot">{revenueData.map((item, index) => <div className="bar-group" key={item.day}><div className="bar-tooltip">${item.value}</div><div className="bars"><i className="sales-bar" style={{ height: `${waste ? 20 + index * 5 : item.value / 14}%` }} /><i className="profit-bar" style={{ height: `${waste ? 12 + index * 3 : item.value / 22}%` }} /></div><span>{t(`dashboard.${days[index]}`)}</span></div>)}</div></div> }
