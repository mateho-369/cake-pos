# The shared self-hosted API is a separate origin in production.
VITE_API_URL=https://api.yourdomain.com

# Set true only for the legacy UI fixture mode. Real deployments use the shared API.
VITE_DEMO_MODE=false
